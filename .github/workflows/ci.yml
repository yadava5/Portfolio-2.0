# Comprehensive CI/CD Pipeline
# Runs on every push to main and on all pull requests
#
# Test Matrix:
#   ┌──────────────────────────────────────────────────────────────────┐
#   │  Stage 1: Quality Checks (parallel)                              │
#   │  ┌─────────────┐ ┌─────────────┐ ┌─────────────┐ ┌─────────────┐ │
#   │  │   Format    │ │   ESLint    │ │  TypeScript │ │  Security   │ │
#   │  │  (Prettier) │ │ (Code Qual) │ │ (Type Check)│ │  (npm audit)│ │
#   │  └──────┬──────┘ └──────┬──────┘ └──────┬──────┘ └──────┬──────┘ │
#   └─────────┼───────────────┼───────────────┼───────────────┼────────┘
#             │               │               │               │
#             └───────────────┴───────┬───────┴───────────────┘
#                                     ▼
#   ┌──────────────────────────────────────────────────────────────────┐
#   │  Stage 2: Build & Analyze                                        │
#   │  ┌─────────────────────────┐ ┌─────────────────────────────────┐ │
#   │  │     Production Build    │ │      Bundle Size Analysis       │ │
#   │  │      (Next.js build)    │ │   (Track JS/CSS bundle sizes)   │ │
#   │  └─────────────────────────┘ └─────────────────────────────────┘ │
#   └──────────────────────────────────────────────────────────────────┘

name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

# Cancel in-progress runs when a new commit is pushed
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  NODE_VERSION: 20

jobs:
  # ═══════════════════════════════════════════════════════════════════
  # STAGE 1: Quality Checks (run in parallel)
  # ═══════════════════════════════════════════════════════════════════

  # ─────────────────────────────────────────────────────────────
  # Format: Check code formatting with Prettier
  # ─────────────────────────────────────────────────────────────
  format:
    name: 🎨 Format (Prettier)
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: "npm"

      - name: Install dependencies
        run: npm ci

      - name: Check formatting
        run: |
          echo "🔍 Checking code formatting with Prettier..."
          npm run format:check
          echo "✅ All files are properly formatted!"

  # ─────────────────────────────────────────────────────────────
  # ESLint: Check code quality and best practices
  # ─────────────────────────────────────────────────────────────
  eslint:
    name: 🔍 ESLint (Code Quality)
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: "npm"

      - name: Install dependencies
        run: npm ci

      - name: Run ESLint
        run: |
          echo "🔍 Checking code quality with ESLint..."
          npm run lint
          echo "✅ No ESLint errors found!"

  # ─────────────────────────────────────────────────────────────
  # TypeScript: Verify type safety
  # ─────────────────────────────────────────────────────────────
  typecheck:
    name: 📝 TypeScript (Type Safety)
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: "npm"

      - name: Install dependencies
        run: npm ci

      - name: Run type check
        run: |
          echo "🔍 Checking TypeScript types..."
          npm run typecheck
          echo "✅ No type errors found!"

  # ─────────────────────────────────────────────────────────────
  # Security: Check for vulnerabilities in dependencies
  # ─────────────────────────────────────────────────────────────
  security:
    name: 🔒 Security (npm audit)
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: "npm"

      - name: Install dependencies
        run: npm ci

      - name: Run security audit
        run: |
          echo "🔍 Checking for security vulnerabilities..."
          npm audit --audit-level=high || echo "⚠️ Some vulnerabilities found (non-blocking)"
          echo "✅ Security check complete!"
        # Note: Using || echo to not fail on moderate vulnerabilities
        # Change to just `npm audit --audit-level=moderate` to be stricter

  # ═══════════════════════════════════════════════════════════════════
  # STAGE 2: Build & Analyze (runs after Stage 1 passes)
  # ═══════════════════════════════════════════════════════════════════

  # ─────────────────────────────────────────────────────────────
  # Build: Production build verification
  # ─────────────────────────────────────────────────────────────
  build:
    name: 🏗️ Build (Next.js)
    runs-on: ubuntu-latest
    needs: [format, eslint, typecheck]
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: "npm"

      - name: Install dependencies
        run: npm ci

      - name: Build application
        run: |
          echo "🏗️ Building Next.js application..."
          npm run build
          echo "✅ Production build successful!"

      - name: Upload build artifacts
        uses: actions/upload-artifact@v4
        with:
          name: static-export
          path: out
          retention-days: 7

  # ─────────────────────────────────────────────────────────────
  # Resume: Verify public resume remains parseable and one page
  # ─────────────────────────────────────────────────────────────
  resume-check:
    name: 📄 Resume Parser Check
    runs-on: ubuntu-latest
    needs: [format, eslint, typecheck]
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: "npm"

      - name: Install dependencies
        run: npm ci

      - name: Install PDF utilities
        run: |
          sudo apt-get update
          sudo apt-get install -y poppler-utils

      - name: Check resume
        run: npm run resume:check

  # ─────────────────────────────────────────────────────────────
  # Static SEO: Verify GitHub Pages export metadata and routes
  # ─────────────────────────────────────────────────────────────
  static-seo:
    name: 🔎 Static SEO Export
    runs-on: ubuntu-latest
    needs: [format, eslint, typecheck]
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: "npm"

      - name: Install dependencies
        run: npm ci

      - name: Build production static export
        run: NODE_ENV=production NEXT_PUBLIC_BASE_PATH=/Portfolio-2.0 npm run build

      - name: Check static SEO metadata
        run: npm run test:seo

      - name: Install Playwright browsers
        run: npx playwright install --with-deps chromium firefox webkit

      - name: Browser metadata smoke
        run: NEXT_PUBLIC_BASE_PATH=/Portfolio-2.0 npx playwright test tests/playwright/static-seo.spec.ts

  # ─────────────────────────────────────────────────────────────
  # Proof: Verify public metrics have source-truth coverage
  # ─────────────────────────────────────────────────────────────
  proof-manifest:
    name: 🧾 Proof Manifest
    runs-on: ubuntu-latest
    needs: [format, eslint, typecheck]
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: "npm"

      - name: Install dependencies
        run: npm ci

      - name: Check proof coverage
        run: npm run test:proof

      - name: Check contrast choreography (every dusk stop AA)
        run: npm run test:contrast

      - name: Check og cards (rendered footer URL casing + freshness)
        run: npm run assets:check-og

      # These four existed and had never run on a pull request. The
      # 2026-08-02 provenance audit is what surfaced it: check-figures.mjs
      # was written in July specifically to stop the run and the data layer
      # drifting apart, and while it sat outside CI they drifted anyway —
      # /evidence shipped 1,145 tests for two days after the receipt beside
      # it had been corrected to 1,168. A gate that does not run is a
      # comment. It now binds 18 figures instead of 7.
      - name: Check headline figures (run prose ⇄ data layer)
        run: npm run test:figures

      - name: Check beat tables (every beat-indexed table agrees)
        run: npm run test:beat-tables

      # Every /#... link into the home page must land on something. Five
      # were dead on nine indexable routes until 2026-08-02, and nothing
      # noticed for two reasons: a hash that matches nothing scrolls to
      # the top and raises no error, and the browser suite builds the
      # OTHER home page (bare `next build`, no build-home.mjs), where the
      # missing ids do exist. This reads the run itself.
      - name: Check home-page anchors resolve
        run: npm run test:anchors

      - name: Check nameplate invariants
        run: npm run test:nameplate

      - name: Check nameplate negative cases
        run: npm run test:nameplate:negative

  # ─────────────────────────────────────────────────────────────
  # Browser Quality: Playwright smoke, reduced motion, performance
  # ─────────────────────────────────────────────────────────────
  browser-quality:
    name: 🎭 Browser Quality Gates
    runs-on: ubuntu-latest
    needs: [format, eslint, typecheck]
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: "npm"

      - name: Install dependencies
        run: npm ci

      - name: Install Playwright browsers
        run: npx playwright install --with-deps chromium firefox webkit

      - name: Check asset budgets
        run: npm run assets:budget

      - name: Run browser smoke
        run: npm run test:e2e:browser-smoke

      # Reads the NORMAL build that browser-smoke just produced, and asserts
      # no probe route reached it. Order is load-bearing: this must follow a
      # build made WITHOUT the probe flag, and must precede the probe suite
      # below, which deliberately builds one that does contain it.
      - name: Probe routes must not ship
        run: npm run test:probe-routes

      - name: Run reduced-motion checks
        run: npm run test:e2e:reduced-motion

      - name: Run performance checks
        run: npm run test:e2e:performance

      # Last, because it is the only step that builds with
      # NEXT_PUBLIC_TEST_PROBES=1 — a probe build left in out/ would make the
      # leak check above read the wrong artifact.
      #
      # `:ci` runs the frame governor and the HELD apparatus (both green on
      # all five engines) and deliberately OMITS text-garnish.spec.ts, which
      # fails one assertion on webkit-desktop. That failure is pre-existing
      # and unrelated: reproduced at HEAD in a clean worktree with none of
      # the probe-route changes applied, so adding it to CI would import a
      # red that predates this step. The local `test:e2e:probes` still runs
      # all three, so the gap is visible rather than quietly dropped. Tracked
      # separately — the spec asserts "zero layout shift" using integer
      # `offsetWidth`/`offsetTop`, and WebKit rounds this element's 128.17px
      # box to 129 where Chromium rounds to 128, so the assertion cannot
      # distinguish a real shift from a rounding boundary.
      - name: Run probe-gated checks
        run: npm run test:e2e:probes:ci

  # ─────────────────────────────────────────────────────────────
  # Bundle Analysis: Track bundle sizes
  # ─────────────────────────────────────────────────────────────
  bundle-analysis:
    name: 📦 Bundle Size Analysis
    runs-on: ubuntu-latest
    needs: [build]
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: "npm"

      - name: Install dependencies
        run: npm ci

      - name: Build with bundle analyzer
        run: npm run build
        env:
          ANALYZE: true

      - name: Report bundle sizes
        run: |
          echo "📦 Bundle Size Report"
          echo "====================="
          echo ""
          echo "📁 .next/static directory:"
          du -sh .next/static 2>/dev/null || echo "  (not available)"
          echo ""
          echo "📁 JavaScript chunks:"
          find .next -name "*.js" -type f -exec du -h {} \; 2>/dev/null | sort -rh | head -10 || echo "  (not available)"
          echo ""
          echo "📁 CSS files:"
          find .next -name "*.css" -type f -exec du -h {} \; 2>/dev/null | sort -rh | head -5 || echo "  (not available)"
          echo ""
          echo "✅ Bundle analysis complete!"

  # ═══════════════════════════════════════════════════════════════════
  # Final Status Check
  # ═══════════════════════════════════════════════════════════════════
  ci-success:
    name: ✅ CI Complete
    runs-on: ubuntu-latest
    needs:
      [
        format,
        eslint,
        typecheck,
        security,
        build,
        resume-check,
        static-seo,
        proof-manifest,
        browser-quality,
        bundle-analysis,
      ]
    if: always()
    steps:
      - name: Check all jobs
        run: |
          echo "📊 CI Pipeline Summary"
          echo "======================"
          echo ""
          echo "Format:    ${{ needs.format.result }}"
          echo "ESLint:    ${{ needs.eslint.result }}"
          echo "TypeCheck: ${{ needs.typecheck.result }}"
          echo "Security:  ${{ needs.security.result }}"
          echo "Build:     ${{ needs.build.result }}"
          echo "Resume:    ${{ needs.resume-check.result }}"
          echo "StaticSEO: ${{ needs.static-seo.result }}"
          echo "Proof:     ${{ needs.proof-manifest.result }}"
          echo "Browser:   ${{ needs.browser-quality.result }}"
          echo "Bundle:    ${{ needs.bundle-analysis.result }}"
          echo ""
          
          if [[ "${{ needs.format.result }}" != "success" ]] || \
             [[ "${{ needs.eslint.result }}" != "success" ]] || \
             [[ "${{ needs.typecheck.result }}" != "success" ]] || \
             [[ "${{ needs.security.result }}" != "success" ]] || \
             [[ "${{ needs.build.result }}" != "success" ]] || \
             [[ "${{ needs.resume-check.result }}" != "success" ]] || \
             [[ "${{ needs.static-seo.result }}" != "success" ]] || \
             [[ "${{ needs.proof-manifest.result }}" != "success" ]] || \
             [[ "${{ needs.browser-quality.result }}" != "success" ]] || \
             [[ "${{ needs.bundle-analysis.result }}" != "success" ]]; then
            echo "❌ CI Pipeline FAILED"
            exit 1
          else
            echo "✅ CI Pipeline PASSED"
          fi
