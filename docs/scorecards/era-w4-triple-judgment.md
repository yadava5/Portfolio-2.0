# W4 — the era faces its judges (2026-07-18)

Three fresh-eyes agents, told to judge cold (prior scores disclosed only as
history), each rebuilding the export and interacting with the live pages:
clicking the audit walk, pressing the gate stamp, hovering citation rows.
The evidence judge additionally recomputed numbers from raw committed
artifacts and curl-checked all 26 external hrefs. Shots in
`docs/design-lab/shots-w4/` (r-*, v-*, e-*; screenshots stay uncommitted).

## Verdicts vs the era bar (90 / 90 / 85)

| Lens | Journey | W4 | Bar | Verdict |
|---|---|---|---|---|
| Recruiter | 68 → 86 | **91** | ≥90 | **cleared** — "advance" formed at ~80s; "best verifiability machinery I've seen at new-grad level" |
| Evidence | 64 → 86 | **91** | ≥90 | **cleared** — "every number I could independently recompute was exact"; 3.504x recomputed from raw CSV; walk arithmetic matched independent DOM counts on all three cases |
| Visitor | 54 → 72 | **79** | ≥85 | **held open** — "concept, copy, and evidence system are portfolio-of-the-year material… the two flagship micro-interactions read at 40% of their intended volume" |

**Era status: two bars cleared; the visitor holds it open by 6.**

## Standing lines worth keeping

- Recruiter: "This is the moment I stop screening and start recruiting"
  (jobtracker method slip + audit walk). "A candidate down-scoping their own
  claim is phone-screen gold" (HELD stamp).
- Evidence: "the one unearned number is stamped and refuses its own tick, and
  the corrections register contains a real retraction the raw data confirms."
- Visitor: the gate press is "the visitor performs the site's entire argument
  in one press" — single strongest moment. Stipple masthead "lovely,
  on-vocabulary" — but it's the only entrance.

## Converged findings (multiple judges, highest priority)

1. **e-07 held/earned contradiction** (all three): the evidence index bundles
   "97%+ accuracy · 3.5x" as one ✓ [public] entry sourced to BENCHMARKS.md —
   but the accuracy is HELD on the case file, BENCHMARKS.md contains no
   accuracy figure, and the receipt routes to row 02 (the earned 3.5x), not
   row 01 (the held 97%). The site disagrees with itself. **P0 honesty.**
2. **automl tick semantics generous** (evidence #2, recruiter #5): poster/deck
   figure references count as "terminating in artifacts" (6 of 8); a
   repo-pinned JSON and a photo of a poster earn the same tick.
3. **"Held" means two opposite things** (evidence #4): home fig. 6.1 legend
   ("held — gate stopped the run", a success) vs the HELD stamp ("not yet
   earned", a deficit).
4. **Audit-walk payoff invisible / below the fold** (visitor #1, recruiter #5):
   faint gutter ticks, control greys out, settled count lands below attention.

## The visitor's six (the 79 → 85 path)

1. Walk payoff: ticks in accent rust; "run the audit →" transforms in place
   into the settled ledger line at the table head.
2. Citation stroke originates at the fig-link (not the left row number);
   left shoulder reserved for walk marks.
3. Hero headline gets an entrance beat (ink-settle) before the byline stipple
   — hierarchy currently inverted. LCP/CLS constraints absolute.
4. Work-row right rail: one primary act (the case file →), source · demo
   inline secondary, last-verified dimmed/unlinked; mono capped at 2 lines.
5. Case-file midsection: t-slips seated as true right-margin sidenotes.
6. (Elevation) The paper's closing "on file:" manifest — render the visitor's
   own persisted audit trail (files opened, walks run, run 041 signed) as a
   margin manifest in stamp rust on ¶07.

Rejected from visitor's ideas: kicker-clock numeral flicker at chapter hinges
— that is a scramble effect, NO-LIST banned. Dawn-grey hero warming deferred
(touches LCP + day-arc contract at scroll-0).

## Recruiter items routed elsewhere

- Flagship thinnest-evidenced (its #1): user content-debt (run ledger
  pending); demoting automl in the hero is the user's product call — surfaced,
  not taken.
- `/evidence/` 404s on production (its #3): PR #5 is unmerged; deploy is the
  user's call. CI curl-check of canonical hrefs → Phase 5 chip.
- 90-second path line beside the skip link + chapter-rail labels: cheap,
  in round A.

## Evidence items acknowledged open (honest as-is, no action this round)

Self-attested ledgers close two chains (policybot, master-inventory) ·
walk verifies build-time state, not live fetches (correct for static export) ·
four rows carry "date not recorded" · SetFit third layer never evaluated
(user content-debt).

## The W5 plan

- **Round A — honesty + machinery**: e-07 split + routing, "refused" legend,
  capture-vs-artifact tick semantics (arithmetic must survive independent
  recount), walk payoff transform, citation re-origin, work-row hierarchy,
  90-second path, rail labels.
- **Round B — entrance + margins + manifest**: hero headline ink-settle
  (LCP ≤ ~300ms, CLS 0.00), t-slip margin seating, "on file:" closing
  manifest.
- **W5 re-judge**: fresh visitor (bar 85) + evidence spot-recount of the
  changed arithmetic. Recruiter/evidence stand at 91 unless regressed.
